{
  "name": "network-pi",
  "version": "1.1.0",
  "description": "Acker testing platform for pi controlled over the network",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm-run-all --parallel start:server start:webapp",
    "start:webapp": "cd app && npm run start -- --open",
    "start:server": "ts-node server/index.ts",
    "start:server:watch": "ts-node-dev server/index.ts --watch",
    "watch": "npm-run-all --parallel start:webapp start:server:watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.14.37",
    "ack-pi": "github:ackerapple/ack-pi",
    "npm-run-all": "^4.1.5",
    "ts-node": "^9.1.1",
    "ws": "^7.4.4"
  },
  "devDependencies": {
    "ts-node-dev": "^1.1.6",
    "typescript": "4.0.5",
    "zone.js": "^0.11.4"
  }
}
